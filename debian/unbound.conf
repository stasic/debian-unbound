# Unbound configuration file for Debian.
#
# See the unbound.conf(5) man page.
#
# See /usr/share/doc/unbound/examples/unbound.conf for a commented
# reference config file.

# If the resolvconf package is installed and RESOLVCONF_FORWARDERS is
# enabled in /etc/default/unbound, the following line will import the
# nameservers discovered by resolvconf (e.g. from DHCP or static entries
# in /etc/network/interfaces) as forwarders.
include: "/var/lib/unbound/forwarders.conf"

server:
    # The following line will configure unbound to perform cryptographic
    # DNSSEC validation using the root trust anchor.
    auto-trust-anchor-file: "/var/lib/unbound/root.key"
